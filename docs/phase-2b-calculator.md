# Phase 2b — Calculator

Build the client-side mortgage calculator. Pure TypeScript math in a composable — no API calls, zero latency. All four calculation types: amortization, scenario comparison, points break-even, rent vs buy.

## Deliverables

- [x] `src/types/calculator.ts` — all input/output interfaces
- [x] `src/composables/useCalculator.ts` — four math functions, fully tested
- [x] `CalculatorView.vue` — reactive UI, three scenario tabs, four result panels

---